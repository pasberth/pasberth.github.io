$(BUILDDIR):
    mkdir -p $(BUILDDIR)

$(BUILDDIR)/js: $(BUILDDIR)
    mkdir -p $(BUILDDIR)/js

$(BUILDDIR)/lib: $(BUILDDIR)
    mkdir -p $(BUILDDIR)/lib

$(BUILDDIR)/jsnedra: $(BUILDDIR)
    mkdir -p $(BUILDDIR)/jsnedra

clean: build
    rmdir $(BUILDDIR)/css
    rmdir $(BUILDDIR)/js
    rmdir $(BUILDDIR)/lib
    rmdir $(BUILDDIR)/jsnedra
    rmdir $(BUILDDIR)/img
