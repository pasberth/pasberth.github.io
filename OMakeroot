BUILDDIR      = build
SPHINXBUILD   = sphinx-build

DefineCommandVars()

BUILDDIR = $(dir $(BUILDDIR))

open build/C
open build/OCaml

.PHONY: all
.SUBDIRS: . gallery blog $(foreach $(fun x => $(dirname $(x))), $(find -name OMakefile))
