\documentclass{article}
\pagestyle{empty}
\begin{document}
$ ([ \alpha \mapsto (Nat \rightarrow Nat), \beta \mapsto Nat ],\lambda f : \left( Nat \rightarrow Nat \right) . \lambda x : Nat . f \left( f x \right)) $
\end{document}