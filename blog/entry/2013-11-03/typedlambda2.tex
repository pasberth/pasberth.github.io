\documentclass{article}
\pagestyle{empty}
\begin{document}
$ ([ \alpha \mapsto (\left( \alpha \rightarrow \beta \right) \rightarrow \alpha \rightarrow \beta), \beta \mapsto (\alpha \rightarrow \beta) ], \lambda f : \left( \left( \alpha \rightarrow \beta \right) \rightarrow \alpha \rightarrow \beta \right) . \lambda x : \alpha \rightarrow \beta . f \left( f x \right)) $
\end{document}